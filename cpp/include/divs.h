#ifndef DIVS_H_
#define DIVS_H_

#include <cstddef>
#include <vector>

typedef unsigned long uint;
typedef unsigned long long uint64;

void get_divs(std::vector<uint>& p, uint64 n);

#endif /* DIVS_H_ */